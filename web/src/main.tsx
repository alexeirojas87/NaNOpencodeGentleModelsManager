// Bootstrap skeleton only — the dashboard UI (left rail, views, save bar)
// lands in WU6. This stub exists so the Vite entry resolves.
import { createRoot } from 'react-dom/client';

const container = document.getElementById('root');
if (container) {
  createRoot(container).render('Model dashboard — UI scaffold lands in WU6.');
}
